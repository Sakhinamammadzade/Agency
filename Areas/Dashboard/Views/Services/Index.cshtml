@model List<Services>
<a class="btn btn-success" href="/Dashboard/Services/Create">Create</a>

<table class="table">
    <thead>
        <tr>
            <th scope="col">#</th>
            <th scope="col">Title</th>
            <th scope="col">Icon</th>
            <th scope="col"></th>
        </tr>
    </thead>
    <tbody>
        @foreach (var item in Model)
        {
            <tr>

                <th scope="row">@item.Id</th>
                <td>@item.Title</td>
                <td>
                    <i class="@item.Icon"></i>
                </td>
                <td>
                    <a href="/dashboard/services/edit/@item.Id" class="btn btn-outline-warning btn-sm">Edit</a>
                    <a href="/dashboard/services/delete/@item.Id" class="btn btn-outline-danger btn-sm">Delete</a>
                    <a class="btn btn-outline-info btn-sm">Detail</a>

                </td>

            </tr>
        }



    </tbody>
</table>




